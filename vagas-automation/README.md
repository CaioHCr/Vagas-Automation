# Executive Job Automation System

MVP for automated job extraction and analysis.

## Structure
- `core/`: System core (scrapers, intelligence, database).
- `app.py`: Streamlit Kanban interface.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Configure `.env`.
3. Run: `streamlit run app.py`
